services:
  - type: web
    name: riftbound-rules-chatbot
    runtime: docker
    plan: free
    healthCheckPath: /health
    envVars:
      - key: RIFTBOUND_VISION_PROVIDER
        value: gemini
      - key: GEMINI_API_KEY
        sync: false
